# Database

Database migrations and seeds live here once the platform moves beyond automatic Phase 0 schema initialization.

The current runnable schema is still initialized by `packages/bff/src/index.js`.
