# Infrastructure

Infrastructure-as-code and deployment manifests live here.

Keep local development in the root `docker-compose.yml` until Terraform, Kubernetes, Helm, or GitOps files are introduced.
